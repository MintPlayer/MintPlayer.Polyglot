// REFUSED (PRD §3.B): a finalizer (`~Foo`) inside a class. The parser recognizes the `~` member form
// and refuses it out loud with a targeted "Polyglot refuses finalizers" diagnostic (issue #54), then
// brace-skips the body so no cascade errors follow.
class Foo {
  ~Foo() {
  }
}
